name: 'Build Image'

on:
  push:
    paths-ignore:
      - '**.md'
    branches-ignore:
      - 'renovate/**'

  release:
    types:
      - published

  workflow_dispatch:

permissions:
  contents: read
  packages: write
  actions: read
  security-events: write

jobs:
  build:
    uses: SlashNephy/.github/.github/workflows/docker-build.yml@2b64691f87d64d627f89946fb6f21169ddffc8b0 # master
    with:
      image-name: ghcr.io/slashnephy/m2ts-classifier
      image-platforms: linux/amd64
